/**
 * A popular misconception regarding reactive programming is that reactive programming is
 * multi-threaded by default.
 *
 * The truth is actually that RxKotlin works on a single thread by
 * default, although it provides us with loads of operators to implement multi-threading as
 * per our business logic and requirements with ease.
 *
 * */