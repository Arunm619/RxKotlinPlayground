/**
 * There are basically two operators that help us implement schedulers. Up until now, in this
 * chapter, we've used the subscribeOn operator in all the examples with a scheduler;
 * however, there's another operator—observeOn. We will now focus on these two operators,
 * learning how they work, and how they differ.
 * */